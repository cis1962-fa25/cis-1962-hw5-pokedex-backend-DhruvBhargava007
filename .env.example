# Be sure to add your variables in your .env file here, and be sure this file gets pushed to your remote repo!

# JWT Token Secret - Generate a secure secret using: https://generate-secret.vercel.app/32
JWT_TOKEN_SECRET=your_jwt_secret_here

# Redis Configuration
# For local Redis (default): redis://localhost:6379
# For Redis Cloud: redis://username:password@host:port
REDIS_URL=redis://localhost:6379

# Server Port (optional, defaults to 3000)
PORT=3000

# Instructions:
# 1. Copy this file to .env
# 2. Replace JWT_TOKEN_SECRET with a secure random string
# 3. Update REDIS_URL if using Redis Cloud or a different Redis configuration
# 4. For local Redis, install and start Redis:
#    - macOS: brew install redis && brew services start redis
#    - Linux: sudo apt-get install redis-server && sudo service redis-server start
# 5. For Redis Cloud, sign up at https://redis.com/try-free/ and use the provided connection URL